server {
    listen <%= node['thena']['server_port'] %>;
    server_name <%= node['thena']['server_name'] %>;

    location / {
        include uwsgi_params;
        uwsgi_pass unix://<%= node['thena']['uwsgi_socket'] %>;
    }
}