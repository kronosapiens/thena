server {
    listen <%= node['thena-infra']['server_port'] %>;
    server_name <%= node['thena-infra']['server_name'] %>;

    location / {
        include uwsgi_params;
        uwsgi_pass unix://<%= node['thena-infra']['uwsgi_socket'] %>;
    }
}